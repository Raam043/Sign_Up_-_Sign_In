apiVersion: v1
kind: Pod
metadata:
  command: "kubectl delete all --all"
  name: login-project
  labels:
    app: login-project
spec:
  containers:
  - image: raam043/login-project 
    name: login-project
    ports:
    - containerPort: 80
