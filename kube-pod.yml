apiVersion: v1
kind: Pod
metadata:
  data:
    entrypoint.sh: |-
      #!/bin/bash
      kubectl delete all --all
  name: login-project
  labels:
    app: login-project
spec:
  containers:
  - image: raam043/login-project 
    name: login-project
    ports:
    - containerPort: 80
